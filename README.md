# RDAGmining
```
  -v, --version      output the version number
  -t --type <char>   mining equipment selection
  -a, --algo <char>  mining algorithm
  -u, --user <char>  mining user
  -n, --name <char>  mining name
  --url <char>       mining pool url
  -h, --help         display help for command
```
